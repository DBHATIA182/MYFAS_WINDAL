# Use YOUR app folder — drive letter does not matter (E:, D:, etc.).
# Example on client: D:\windal\apptest   Example on dev PC: E:\WINDAL\APPTEST

cd D:\windal\apptest
# Or:  cd E:\WINDAL\APPTEST

# Tunnel dal-demo ALREADY EXISTS? Use -SkipClientSetup only to refresh config.yml + JSON on this PC
# (or copy config.yml + <uuid>.json from the machine where tunnel was created).

# A) Link existing tunnel + write config.yml + JSON (no new tunnel if name exists):
powershell -ExecutionPolicy Bypass -File .\setup-new-client-tunnel.ps1 `
  -ClientKey "dal-demo" `
  -SkipClientSetup `
  -OraclePrimaryUser "DAL" `
  -OraclePrimaryPassword "DAL" `
  -OracleSecondaryUser "DAL" `
  -OracleSecondaryPassword "DAL" `
  -OracleConnectString "XE"

# B) Full client setup (npm + task FAS-dal-demo-API) — PowerShell as Administrator:
# powershell -ExecutionPolicy Bypass -File .\setup-new-client-tunnel.ps1 `
#   -ClientKey "dal-demo" `
#   -OraclePrimaryUser "DAL" ...

# Public URLs (tunnel name dal-demo — not fas-dal-demo):
# - https://dal-demo.fasaccountingsoftware.in
# - https://dal-demo-api.fasaccountingsoftware.in

# Then start stack (from the same APP folder you used above):
#   npm.cmd run server
#   npm.cmd run dev -- --host 0.0.0.0 --port 5174
#   cloudflared tunnel --config .\config.yml run
