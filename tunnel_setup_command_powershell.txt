# WINDAL tunnel — run from APPTEST folder (E:\WINDAL\APPTEST or D:\windal\apptest)
# In PowerShell always use .\ before .cmd files in the current folder.

cd E:\WINDAL\APPTEST

# 1) Login (once)
.\setup-windal-tunnel.cmd login

# 2) Create tunnel + routes + config.yml
.\setup-windal-tunnel.cmd dal-srfipulses

# 2b) Routes only (tunnel already exists)
.\setup-windal-tunnel.cmd dal-srfipulses routes

# 3) Start API + web + tunnel
.\start-windal-stack.cmd

# --- Or from CMD.exe (no .\ needed) ---
# setup-windal-tunnel.cmd login
# setup-windal-tunnel.cmd dal-srfipulses

# --- PowerShell .ps1 single lines ---
powershell -ExecutionPolicy Bypass -File .\setup-windal-tunnel.ps1 -Login
powershell -ExecutionPolicy Bypass -File .\setup-windal-tunnel.ps1 -ClientKey dal-srfipulses

# cloudflared on this PC:
#   C:\Program Files (x86)\Cloudflared\cloudflared.exe

# URLs:
#   https://dal-srfipulses.fasaccountingsoftware.in
#   https://dal-srfipulses-api.fasaccountingsoftware.in
